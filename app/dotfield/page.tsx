// A bare showcase of the dot field — no case-study content, so there's
// nothing to compete with it. The field itself is the global background
// canvas already mounted by the root layout (see `<DotField viewport />`
// in app/layout.tsx); this page just leaves room for it to fill the frame.
export default function DotFieldPage() {
  return <div className="min-h-screen" aria-hidden="true" />
}
